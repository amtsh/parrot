/// Overwritten by .github/workflows/release.yml with the actual tag
/// (e.g. "v0.0.8") right before a release build. Local/dev builds keep
/// "dev", which UpdateChecker treats as "never check."
let parrotVersion = "dev"
